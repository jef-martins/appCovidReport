import { RelatorioPipe } from './pipes/data.pipe';

describe('RelatorioPipe', () => {
  it('create an instance', () => {
    const pipe = new RelatorioPipe();
    expect(pipe).toBeTruthy();
  });
});
